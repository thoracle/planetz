// Quick 3D Proximity Detector Test - Copy this entire line into console:

console.log('🎯 Quick 3D Proximity Detector Test (Optimized)'); const s = window.starfieldManager; if (!s) { console.error('❌ Game not loaded'); } else { const d = s.proximityDetector3D; console.log(`3D Detector: ${d ? '✅' : '❌'}`); if (d) { console.log(`Scene: ${d.scene ? '✅' : '❌'}`); console.log(`Camera: ${d.camera ? '✅' : '❌'}`); console.log(`Grid: ${d.gridMesh ? '✅' : '❌'}`); console.log(`Player indicator: ${d.playerIndicator ? '✅' : '❌'}`); console.log(`Config: ${d.config.gridSize}x${d.config.gridSize} grid, ${d.config.gridTilt}° tilt, ${(d.config.detectionRange/1000).toFixed(0)}km range, ${d.config.gridSpacing}m spacing`); console.log(`Size: ${(d.config.screenWidth*100).toFixed(1)}%x${(d.config.screenHeight*100).toFixed(1)}% (wider/shorter), Camera: ${d.config.cameraDistance} units (zoomed)`); const ship = s.viewManager?.getShip(); if (ship) { const hasCards = ship.hasSystemCardsSync?.('radar'); console.log(`Has cards: ${hasCards ? '✅' : '❌'}`); if (hasCards) { console.log('💡 Toggling 3D proximity detector...'); if (!d.isVisible) s.toggleProximityDetector(); console.log('🔄 Forcing detector update to find objects...'); d.forceUpdate(); console.log('🎯 Check BOTTOM-CENTER of screen for zoomed 3D display (reduced console spam)!'); } else { console.log('❌ No proximity detector cards installed'); } } } }